{
  "private": true,
  "name": "@deltachat-desktop/shared",
  "type": "module",
  "scripts": {
    "check:types": "tsc --noEmit",
    "pretest": "esbuild *.ts --outdir=ts-compiled-for-tests --sourcemap",
    "test": "cross-env NODE_OPTIONS=--enable-source-maps mocha 'tests/**/*.js'"
  },
  "dependencies": {
    "@deltachat/jsonrpc-client": "catalog:",
    "error-stack-parser": "^2.1.4",
    "stackframe": "^1.3.4"
  },
  "devDependencies": {
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "chai": "^5.3.3",
    "esbuild": "^0.25.12",
    "mocha": "^10.8.2"
  }
}
